import { FilterSimplePipe } from './filter-simple.pipe';

describe('FilterSimplePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSimplePipe();
    expect(pipe).toBeTruthy();
  });
});
